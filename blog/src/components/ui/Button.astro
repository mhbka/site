---
import type { HTMLAttributes } from 'astro/types';

type Props = HTMLAttributes<'button'>;

const { class: className, ...props } = Astro.props;
---

<button class:list={['ui-button', className]} {...props}>
	<slot />
</button>
