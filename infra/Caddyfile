{
	# TLS certificates are obtained automatically after DNS points at the instance.
	email {$ACME_EMAIL}
}

{$SITE_DOMAIN} {
	reverse_proxy frontend:4321
}

{$API_DOMAIN} {
	reverse_proxy backend:8080
}
